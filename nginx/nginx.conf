upstream bicycle {
    # service name in docker-compose file  and 0.0.0.0:9000->8000/tcp port : 8000 is inner container(blog_web) port.
    #server blog_web:8000;
    server web.bicycle:8000;
}

server {

    listen 80;
    listen 443;

    location / {
        proxy_pass http://bicycle;
        #proxy_pass https://bicycle;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

}




# /etc/nginx/sites-available/blog_project.conf
#  sudo ln -s /etc/nginx/sites-available/blog_api.conf  /etc/nginx/sites-enabled/

# Upstream block for the REST API backend
# upstream api_backend {
#     server 0.0.0.0:8000;  # Replace with the address of your REST API server
# }


# upstream api2_backend {
#     server 0.0.0.0:8001;  # Replace with the address of your REST API server
# }

# server {
#     listen 80;
#     server_name 13.233.83.246;  # Replace with your domain name or IP address

#     # Location block for the React frontend
#     #location / {
#     #    root /path/to/your/react-app/build;  # Replace with the path to your React app build directory
#     #    try_files $uri $uri/ /index.html;
#     #}

#     # Location block for proxying requests to the REST API backend


#     location /api2/ {
#         proxy_pass http://api2_backend/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }




#     location /api/ {
#         proxy_pass http://api_backend/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }